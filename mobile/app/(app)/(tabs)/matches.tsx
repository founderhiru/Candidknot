import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function MatchesScreen() {
  return (
    <PlaceholderScreen
      title="Matches"
      note="Interests and matches arrive in a later phase."
    />
  );
}
