# Same reason as the root app's .npmrc: @better-auth/expo pins zod ^4.5.4
# internally, while this project uses zod 3.x for its own type-only usage
# (see src/lib/api-client.ts). Independent node_modules from the root Next.js
# app, but the same npm peer-resolution conflict applies here too.
legacy-peer-deps=true
